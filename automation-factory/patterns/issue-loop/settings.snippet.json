{
  "//": "issue-loop · Module 1 wiring. Merge this into the TARGET repo's .claude/settings.json (not the-grid's). Replace {{WORKING_DIR}} with the target repo's absolute path. The hook fires after every Bash call and self-filters to git commits.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "{{WORKING_DIR}}/.claude/hooks/post-commit-review.sh"
          }
        ]
      }
    ]
  }
}
