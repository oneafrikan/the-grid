# Copy to .env and fill in. Never commit .env itself.
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASS=

# DB_NAME and APP_PORT are worktree-specific — generated into .env.local by
# scripts/worktree-setup.sh. Do not set them here.
