<!-- rejected-hashes: -->

# Learnings

Durable lessons mined from this project's own history (commits, `handoffs/`,
docs). Generated and maintained by the `mine-learnings` skill — don't
hand-edit the hash comments, they're the dedup ledger that keeps reruns
idempotent. The highest-value entries here are also folded as short rules
into `CLAUDE.md`.

(empty — no learnings mined yet)
