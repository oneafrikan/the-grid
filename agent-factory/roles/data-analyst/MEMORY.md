<!--
  MEMORY.md — Data Analyst role memory seed (flat-file / signals model).
  This file is APPENDED to _core/MEMORY_base.md at compose time.
  Headings match MEMORY_base.md where they overlap so the merge reads cleanly.
  Phase-1: plain markdown. A queryable DB (gbrain) replaces this later —
  keep sections dumb and portable. No SQL, no structured query syntax.

  A specialist CONSUMES stack/source decisions (set by the Tech Lead / Data
  Engineer); it records the metric definitions, trusted sources, and findings it
  relies on. Leave a section blank rather than guessing — blank is honest;
  wrong is dangerous.
-->

# Memory (Data Analyst seed)

## Metric definitions

<!--
  Agreed definition of each metric this analyst reports. The source of truth so a
  number means the same thing across sessions.
  Format: - **<Metric>** = <exact definition> (source: <table>, decided YYYY-MM-DD)
-->

_None yet._

## Trusted data sources

<!--
  Sources this analyst has validated and relies on. Records freshness behaviour
  and known gaps so they're not re-discovered each session.
  Format: - **<source>** — <what it holds> | freshness: <cadence> | known gaps: <notes>
-->

_None yet._

## Active work

<!-- What is in flight. Format: - **<Question>** — decision: <what it informs> | Status: <status> -->

_None yet._

## Recent findings

<!-- Dated log of answers reported and what they showed. Append; do not overwrite.
     Format: - YYYY-MM-DD: <finding> — <answer + key caveat> -->

_None yet._

## Learned preferences

<!-- Observed preferences of the operator — rigour vs speed, how much to caveat,
     preferred report shape, how much to ask vs proceed. Accumulated over sessions. -->

_None yet._
