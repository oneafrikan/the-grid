<!--
  MEMORY.md — Backend Dev role memory seed (flat-file / signals model).
  This file is APPENDED to _core/MEMORY_base.md at compose time.
  Headings match MEMORY_base.md where they overlap so the merge reads cleanly.
  Phase-1: plain markdown. A queryable DB (gbrain) replaces this later —
  keep sections dumb and portable. No SQL, no structured query syntax.

  A specialist CONSUMES stack/architecture decisions (set by the Tech Lead);
  it records the contracts it has shipped and the conventions it must follow.
  Leave a section blank rather than guessing — blank is honest; wrong is dangerous.
-->

# Memory (Backend Dev seed)

## Stack decisions

<!-- Pinned tech choices this backend must build against (set by the Tech Lead).
     Consulted before writing code so it stays consistent. -->

<!-- STACK: stack-specific defaults (language, framework, DB, ORM) injected here -->

_No stack decisions pinned yet._

## Architecture patterns

<!-- Conventions this codebase follows. Consulted before new work so it fits
     the shape already established (layering, error format, validation rules). -->

<!-- STACK: stack-specific patterns (file layout, naming, framework idioms) injected here -->

_No patterns recorded yet._

## API contracts shipped

<!--
  Endpoints this backend has shipped — the source of truth for the frontend.
  Format: - `<METHOD> <path>` → request / response / status codes (PRD: <path>)
-->

_No contracts shipped yet._

## Active work

<!-- What is in flight. Format: - **<Task>** — PRD: <path> | Status: <status> | PR: #<n> -->

_Nothing in flight yet._

## Recent decisions

<!-- Dated log of what changed and why. Append; do not overwrite.
     Format: - YYYY-MM-DD: <what changed> — <why> -->

_No decisions logged yet._

## Learned preferences

<!-- Observed preferences of the operator — how much to ask vs proceed, PR shape,
     test depth. Accumulated over sessions. -->

_No preferences logged yet._
