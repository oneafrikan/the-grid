<!--
  MEMORY.md — Growth Hacker role memory seed (flat-file / signals model).
  This file is APPENDED to _core/MEMORY_base.md at compose time.
  Headings match MEMORY_base.md where they overlap so the merge reads cleanly.
  Phase-1: plain markdown. A queryable DB (gbrain) replaces this later —
  keep sections dumb and portable. No SQL, no structured query syntax.

  A specialist experimenter records its funnel baseline and every experiment it
  has run — won or lost — so it doesn't re-run dead ideas. Leave a section blank
  rather than guessing — blank is honest; wrong is dangerous.
-->

# Memory (Growth Hacker seed)

## Funnel baseline

<!-- The funnel steps and their current conversion/drop-off rates — the source
     of truth for picking the bottleneck and reading any lift.
     Format: - <step A → step B>: <rate>% (as of YYYY-MM-DD) -->

_None yet._

## Experiments run (won / lost)

<!-- Every completed experiment, won or lost. A logged loss is a learned
     constraint; an unlogged loss gets re-run.
     Format: - YYYY-MM-DD <name> — hypothesis: <one line> | result: WON/LOST/INCONCLUSIVE (<lift>, <sig>) | learning: <one line> -->

_None yet._

## Active experiments

<!-- What is running right now. Read guardrails before reading the result.
     Format: - **<name>** — metric: <metric> | arms: <n> | ends: YYYY-MM-DD | threshold: <lift> -->

_None yet._

## Recent decisions

<!-- Dated log of ship/kill/iterate calls and why. Append; do not overwrite.
     Format: - YYYY-MM-DD: <decision> on <experiment> — <why> -->

_None yet._

## Learned preferences

<!-- Observed preferences of the operator — risk tolerance, how decisive a result
     must be to ship, spend ceiling, how much to ask vs proceed. Accumulated over sessions. -->

_None yet._
