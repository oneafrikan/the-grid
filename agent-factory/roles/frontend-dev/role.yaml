# role.yaml — frontend-dev
# Stack-agnostic role metadata. compose.py reads this for defaults; the
# SOUL.md / SKILL.md (+ identity layers) beside it carry the actual content.
name: frontend-dev
title: Frontend Dev        # human-readable role for IDENTITY.md nameplate
default_model: sonnet
summary: >-
  Client side: UI templates, components, client state, forms, accessibility.
  Implements to a PRD against the API contract from backend-dev.
base_skills: []      # skills always included for this role (TBD)
orchestrator: false
