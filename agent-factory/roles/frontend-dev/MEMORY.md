<!--
  MEMORY.md — Frontend Dev role memory seed (flat-file / signals model).
  This file is APPENDED to _core/MEMORY_base.md at compose time.
  Headings match MEMORY_base.md where they overlap so the merge reads cleanly.
  Phase-1: plain markdown. A queryable DB (gbrain) replaces this later —
  keep sections dumb and portable. No SQL, no structured query syntax.

  A specialist CONSUMES stack/architecture decisions (set by the Tech Lead) and
  the API contracts (set by backend-dev); it records the components it has
  shipped and the conventions it must follow.
  Leave a section blank rather than guessing — blank is honest; wrong is dangerous.
-->

# Memory (Frontend Dev seed)

## Stack decisions

<!-- Pinned tech choices this frontend must build against, consumed from the Tech
     Lead's decisions. Consulted before writing code so it stays consistent. -->

<!-- STACK: stack-specific defaults (framework, state library, styling, test tools) injected here -->

_No stack decisions consumed yet._

## Components shipped

<!--
  Components/views this frontend has shipped and the contract each builds against.
  Format: - `<Component>` → consumes `<METHOD> <path>` | states: empty/loading/error/success (PRD: <path>)
-->

_No components shipped yet._

## Active work

<!-- What is in flight. Format: - **<Task>** — PRD: <path> | Status: <status> | PR: #<n> -->

_Nothing in flight yet._

## Recent decisions

<!-- Dated log of what changed and why. Append; do not overwrite.
     Format: - YYYY-MM-DD: <what changed> — <why> -->

_No decisions logged yet._

## Learned preferences

<!-- Observed preferences of the operator — how much to ask vs proceed, PR shape,
     a11y depth, design polish vs speed. Accumulated over sessions. -->

_No preferences logged yet._
