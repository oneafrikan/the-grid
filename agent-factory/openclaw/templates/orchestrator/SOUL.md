<!--
Thin passthrough template. Session B2's write_openclaw() fills this from
compose.py's render_agent(agent)["SOUL.md"] (the existing _core/SOUL_base.md +
role-layer merge, unchanged from the claude-code target) — no new merge logic
lives here. See openclaw/README.md for the token list.

Regenerated from source every run; read-only in the deployed workspace
(mode 440) per B2's file-permission scheme.
-->
{{RENDERED_CONTENT}}
