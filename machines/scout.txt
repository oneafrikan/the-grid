# Machine overlay — scout (Jarvis / OpenClaw automation instance).
#
# Layered ON TOP of baseline-submodules.txt. wire.sh resolves this file by
# `hostname -s` (= "scout"). Same entry grammar as the baseline:
#
#   repo-name            → also wire ALL skills in that submodule (additive)
#   repo-name/skill      → also wire ONLY that skill (additive)
#   project:<name>       → also wire a composed agent-factory project
#   -repo | -repo/skill  → REMOVE a baseline entry on this machine
#   -project:<name>      → un-gate a baseline project on this machine
#
# Apply with: bash ~/.the-grid/scripts/wire.sh
#
# Profile: full team-based automation. Gets the full baseline + OpenClaw tools
# + data/ML pack for automation pipelines + personal tools.
# No HubSpot (personal CRM stays on wilderness).

# --- OpenClaw + gbrain management ---
gstack/gstack-openclaw-ceo-review
gstack/gstack-openclaw-investigate
gstack/gstack-openclaw-office-hours
gstack/gstack-openclaw-retro
gstack/gstack-upgrade
gstack/setup-gbrain
gstack/sync-gbrain

# --- data engineering + ML (automation pipelines) ---
gstack/benchmark-models
jeffallan/cloud-architect
jeffallan/fine-tuning-expert
jeffallan/microservices-architect
jeffallan/ml-pipeline
jeffallan/monitoring-expert
jeffallan/pandas-pro
jeffallan/playwright-expert
jeffallan/python-pro
jeffallan/rag-architect
jeffallan/sre-engineer

# --- personal tools ---
mattpocock/obsidian-vault
